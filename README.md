# ⭐ **GitHub Summary (KNN Classification)**

**K-Nearest Neighbors (KNN) Classification Project using Scikit-learn.**
This project demonstrates:

* Feature normalization for fair distance calculations
* Hyperparameter tuning to select the best K
* Accuracy evaluation and confusion matrix visualization
* Decision boundary plotting (using first 2 features)

**Key Concepts:** KNN algorithm, distance metrics, overfitting/underfitting, multi-class handling.

**Tools & Libraries:** Python, Scikit-learn, Pandas, NumPy, Matplotlib
